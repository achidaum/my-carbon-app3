import streamlit as st
from streamlit_gsheets import GSheetsConnection
import pandas as pd
from datetime import datetime

# 1. ตั้งค่าการเชื่อมต่อและ Page
st.set_page_config(page_title="Carbon Tracker Pro", layout="centered")
conn = st.connection("gsheets", type=GSheetsConnection)

# 2. ดีไซน์ CSS
st.markdown("""
    <style>
    .main { background: linear-gradient(135deg, #f0fdf4 0%, #dcfce7 100%); }
    .stAlert { border-radius: 15px; background-color: rgba(255, 255, 255, 0.7); }
    h1 { color: #065f46 !important; text-align: center; }
    div.stButton > button:first-child {
        background: linear-gradient(90deg, #10b981 0%, #059669 100%);
        color: white; border-radius: 12px; border: none; width: 100%; font-weight: bold;
        height: 3em;
    }
    </style>
    """, unsafe_allow_html=True)

st.markdown("<h1>🌿 Carbon Daily Tracker</h1>", unsafe_allow_html=True)

st.info("""
**ที่มาของกิจกรรมนี้:** รายงานฉบับนี้จัดทำขึ้นภายใต้โครงการการประเมินและส่งเสริมการลดก๊าซเรือนกระจกระดับบุคคล 
เพื่อสร้างความ **"ตระหนัก"** รู้และเป็นเครื่องมือช่วยให้บุคคลทั่วไปประเมินการปล่อยก๊าซเรือนกระจก 
อ้างอิงฐานข้อมูลที่เชื่อถือได้จาก **อบก.** เพื่อนำไปสู่การ **"ชดเชย"** หรือปรับลดในอนาคต
""")

# --- ส่วนรับข้อมูล ---
col1, col2 = st.columns(2)
with col1:
    st.markdown("#### 🚗 การเดินทาง")
    transport = st.selectbox("เลือกประเภทพาหนะ", 
        ["รถยนต์ส่วนตัว (น้ำมัน)", "รถยนต์ไฟฟ้า (EV)", "รถจักรยานยนต์", "รถไฟฟ้า (BTS/MRT)", "รถเมล์", "เดิน/ปั่นจักรยาน"])
    distance = st.number_input("ระยะทาง (กิโลเมตร)", min_value=0.0, step=1.0)
with col2:
    st.markdown("#### 🍔 อาหาร")
    food_input = st.text_input("วันนี้ทานเมนูอะไร?", placeholder="เช่น ข้าวกะเพราเนื้อวัว")
    st.markdown("#### 💡 พลังงาน")
    ac_hours = st.slider("เปิดแอร์กี่ชั่วโมง?", 0, 12, 0)

# --- ส่วนประมวลผล ---
if st.button("ประเมินผลลัพธ์"):
    ef_map = {
        "รถยนต์ส่วนตัว (น้ำมัน)": 0.218, "รถยนต์ไฟฟ้า (EV)": 0.05, 
        "รถจักรยานยนต์": 0.08, "รถไฟฟ้า (BTS/MRT)": 0.02, 
        "รถเมล์": 0.03, "เดิน/ปั่นจักรยาน": 0
    }
    carbon_transport = distance * ef_map[transport]
    
    food_carbon = 1.2
    food_text = food_input.lower()
    if "เนื้อวัว" in food_text:
        food_carbon = 7.5
    elif any(x in food_text for x in ["ผัก", "เจ", "มังสวิรัติ"]):
        food_carbon = 0.4
    
    total = carbon_transport + food_carbon + (ac_hours * 0.51)
    
    # แสดงผล Card (แก้ไขเครื่องหมายปิดครบถ้วน)
    st.markdown(f"""
        <div style="background: white; padding: 25px; border-radius: 20px; border-left: 10px solid #10b981; box-shadow: 0 10px 25px rgba(0,0,0,0.05
