import streamlit as st

# ตั้งค่าหน้าแอป
st.set_page_config(page_title="Carbon Tracker Rating", layout="centered")

# หัวข้อหลัก
st.markdown("<h1 style='text-align: center; color: #059669;'>🌍 วันนี้คุณปล่อยคาร์บอนระดับไหน?</h1>", unsafe_allow_html=True)
st.caption("ระบบประเมินเรทการปล่อยคาร์บอนตามเกณฑ์ อบก. (TGO)")

# ส่วนที่ 1: การเดินทาง
st.subheader("🚗 1. การเดินทาง")
transport = st.selectbox("เลือกประเภทพาหนะ", 
    ["รถยนต์ส่วนตัว (น้ำมัน)", "รถยนต์ไฟฟ้า (EV)", "รถจักรยานยนต์", "รถไฟฟ้า (BTS/MRT)", "รถเมล์", "เดิน/ปั่นจักรยาน"])
distance = st.number_input("ระยะทางทั้งหมด (กิโลเมตร)", min_value=0.0, step=1.0, value=0.0)

# ส่วนที่ 2: อาหาร (พิมพ์ชื่อเมนูเอง)
st.subheader("🍔 2. การบริโภคอาหาร")
food_input = st.text_input("วันนี้คุณทานเมนูอะไร?", placeholder="เช่น ข้าวกะเพราเนื้อ, สลัดผัก")

# ส่วนที่ 3: ไฟฟ้า
st.subheader("💡 3. การใช้พลังงาน")
ac_hours = st.slider("เปิดแอร์กี่ชั่วโมง?", 0, 12, 0)

if st.button("ประเมินผลลัพธ์"):
    # --- คำนวณค่าอาหาร ---
    food_carbon = 1.2
    food_name = food_input.lower()
    if "เนื้อวัว" in food_name or "เนื้อ" in food_name and not any(x in food_name for x in ["ไก่", "หมู", "ปลา"]):
        food_carbon = 7.5
    elif any(x in food_name for x in ["ผัก", "สลัด", "เจ", "มังสวิรัติ"]):
        food_carbon = 0.4
    
    # --- คำนวณรวม ---
    ef_map = {"รถยนต์ส่วนตัว (น้ำมัน)": 0.21, "รถยนต์ไฟฟ้า (EV)": 0.05, "รถจักรยานยนต์": 0.08, "รถไฟฟ้า (BTS/MRT)": 0.02, "รถเมล์": 0.03, "เดิน/ปั่นจักรยาน": 0}
    total = (distance * ef_map[transport]) + food_carbon + (ac_hours * 0.51)
    
    # --- ส่วนการแสดงเรท (Rating) ---
    st.markdown("---")
    st.markdown(f"### ผลการประเมิน: **{total:.2f} kgCO2e**")
    
    if total <= 3.0:
        st.success("🌟 **ระดับ: ดีเยี่ยม (Low Carbon Hero)**")
        st.info("คุณใช้ชีวิตที่เป็นมิตรต่อโลกมาก! ช่วยลดก๊าซเรือนกระจกได้อย่างดีเยี่ยม")
        progress_color = "green"
    elif total <= 8.0:
        st.warning("⚠️ **ระดับ: กำลังดี (Moderate)**")
        st.write("การใช้ชีวิตของคุณอยู่ในเกณฑ์มาตรฐานทั่วไป แต่ยังมีส่วนที่ลดได้อีกนะ")
        progress_color = "orange"
    else:
        st.error("🚨 **ระดับ: ปล่อยคาร์บอนสูง (High Emission)**")
        st.write("วันนี้คุณปล่อยคาร์บอนค่อนข้างเยอะ ลองปรับเปลี่ยนพฤติกรรมในวันพรุ่งนี้ดูนะ!")
        progress_color = "red"

    # แสดงคำแนะนำตามพฤติกรรม
    st.markdown("#### 💡 คำแนะนำเฉพาะคุณ:")
    if food_carbon >= 7.5:
        st.write("- **อาหาร:** เมนูเนื้อวัวส่งผลเสียต่อโลกมากที่สุด ลองเปลี่ยนเป็นเนื้อประเภทอื่นในวันพรุ่งนี้")
    if ac_hours >= 8:
        st.write("- **ไฟฟ้า:** เปิดแอร์นานเกินไป ลองปรับมาใช้พัดลมในช่วงที่อากาศไม่ร้อนจัด")
    if transport == "รถยนต์ส่วนตัว (น้ำมัน)" and distance > 20:
        st.write("- **เดินทาง:** ระยะทางไกลด้วยรถน้ำมันปล่อยคาร์บอนเยอะ ลองใช้รถสาธารณะช่วยได้")

st.markdown("---")
st.caption("หมายเหตุ: เกณฑ์การประเมิน 0-3 (ดีเยี่ยม), 3-8 (กลาง), 8+ (สูง) เป็นการประมาณผลเพื่อให้ผู้ใช้ตระหนักถึงการปล่อยก๊าซฯ")
