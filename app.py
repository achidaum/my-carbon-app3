import streamlit as st

# --- 1. ตั้งค่าหน้าตาแอป ---
st.set_page_config(page_title="Thai Carbon Daily Tracker", layout="centered")

# ปรับแต่งดีไซน์ด้วย CSS (แก้ไขจุดที่ทำให้เกิด Error ในรูปก่อนหน้าแล้ว)
st.markdown("""
    <style>
    .main { background-color: #f8fafc; }
    h1 { color: #15803d; text-align: center; }
    div.stButton > button:first-child {
        background: linear-gradient(90deg, #22c55e 0%, #16a34a 100%);
        color: white; border-radius: 12px; border: none; width: 100%; font-weight: bold; height: 3.5em;
    }
    .result-card {
        background: white; padding: 25px; border-radius: 20px; 
        border-top: 10px solid #22c55e; box-shadow: 0 10px 25px rgba(0,0,0,0.05);
        text-align: center;
    }
    .advice-card {
        background: #ffffff; padding: 15px; border-radius: 12px;
        border-left: 5px solid #10b981; margin-bottom: 10px;
    }
    </style>
    """, unsafe_allow_html=True)

st.markdown("<h1>🌿 Thai Carbon Daily Tracker</h1>", unsafe_allow_html=True)

st.info("**เป้าหมาย:** เพื่อให้คุณตระหนักถึงการปล่อยคาร์บอนต่อวัน (อิงเกณฑ์คนไทย 10.4 kgCO2e)")

# --- 2. ส่วนรับข้อมูลจากผู้ใช้ ---
col1, col2 = st.columns(2)
with col1:
    st.markdown("#### 🚗 การเดินทาง")
    transport = st.selectbox("พาหนะหลักวันนี้", 
        ["รถยนต์ส่วนตัว (น้ำมัน)", "รถยนต์ไฟฟ้า (EV)", "รถจักรยานยนต์", "รถไฟฟ้า (BTS/MRT)", "รถเมล์", "เดิน/ปั่นจักรยาน"])
    distance = st.number_input("ระยะทาง (กิโลเมตร)", min_value=0.0, step=1.0, value=10.0)
    
    st.markdown("#### 💡 พลังงาน")
    # จำกัดสูงสุด 12 ชั่วโมงตามคำขอ
    ac_hours = st.slider("เปิดแอร์วันนี้ (ชั่วโมง)", 0, 12, 0)

with col2:
    st.markdown("#### 🍽️ การบริโภคอาหาร (3 มื้อ)")
    food_1 = st.text_input("มื้อเช้า", placeholder="เช่น ข้าวหมูแดง")
    food_2 = st.text_input("มื้อกลางวัน", placeholder="เช่น กะเพราเนื้อวัว")
    food_3 = st.text_input("มื้อเย็น", placeholder="เช่น สลัดผัก")

st.markdown("<br>", unsafe_allow_html=True)

# --- 3. ส่วนประมวลผล (แก้ไข Logic และตัวแปรทั้งหมดให้ถูกต้อง) ---
if st.button("ประเมินผลลัพธ์"):
    # 1. คำนวณคาร์บอนเดินทาง
    ef_map = {
        "รถยนต์ส่วนตัว (น้ำมัน)": 0.218, 
        "รถยนต์ไฟฟ้า (EV)": 0.05, 
        "รถจักรยานยนต์": 0.08, 
        "รถไฟฟ้า (BTS/MRT)": 0.02, 
        "รถเมล์": 0.03, 
        "เดิน/ปั่นจักรยาน": 0
    }
    carbon_t = distance * ef_map[transport]
    
    # 2. ฟังก์ชันคำนวณอาหาร
    def calc_food(text):
        if not text: return 0.0
        t = text.lower()
        if any(x in t for x in ["เนื้อวัว", "เนื้อ", "beef"]): return 7.5
        if any(x in t for x in ["ผัก", "สลัด", "เจ", "มังสวิรัติ"]): return 0.4
        return 1.2
    
    carbon_f = calc_food(food_1) + calc_food(food_2) + calc_food(food_3)
    carbon_e = ac_hours * 0.51
    
    total = carbon_t + carbon_f + carbon_e

    # --- แสดงผล Card ผลลัพธ์ ---
    st.markdown(f"""
        <div class="result-card">
            <h3 style="margin:0; color: #4b5563;">ปริมาณคาร์บอนรวมของคุณวันนี้</h3>
            <h1 style="margin:0; color: #16a34a; font-size: 50px;">{total:.2f} <span style="font-size: 20px; color: #9ca3af;">kgCO2e</span></h1>
            <p style="color: #6b7280;">ค่าเฉลี่ยคนไทยอยู่ที่ประมาณ 10.4 kg ต่อวัน</p>
        </div>
    """, unsafe_allow_html=True)

    if total <= 6.0:
        st.success("🟢 **ระดับ: ดีเยี่ยม** - คุณปล่อยคาร์บอนต่ำกว่าค่าเฉลี่ยมาก!")
    elif total <= 11.0:
        st.warning("🟡 **ระดับ: ปานกลาง** - คุณอยู่ในเกณฑ์มาตรฐานคนไทยทั่วไป")
    else:
        st.error("🚨 **ระดับ: ปล่อยก๊าซสูง** - กิจกรรมวันนี้ปล่อยก๊าซสูงกว่าค่าเฉลี่ย")

    # --- 4. ส่วนแนวทางลดและชดเชย (ที่นำกลับมาให้ครบถ้วน) ---
    st.markdown("---")
    st.markdown("### 💡 แนวทางลดการปล่อยก๊
