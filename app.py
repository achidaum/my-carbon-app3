import streamlit as st

# --- 1. ตั้งค่าหน้าตาแอป ---
st.set_page_config(page_title="Thai Carbon Daily Tracker", layout="centered")

st.markdown("""
    <style>
    .main { background-color: #f8fafc; }
    h1 { color: #15803d; text-align: center; font-family: 'Sarabun', sans-serif; }
    div.stButton > button:first-child {
        background: linear-gradient(90deg, #22c55e 0%, #16a34a 100%);
        color: white; border-radius: 12px; border: none; width: 100%; font-weight: bold; height: 3.5em;
    }
    .result-card {
        background: white; padding: 25px; border-radius: 20px; 
        border-top: 10px solid #22c55e; box-shadow: 0 10px 25px rgba(0,0,0,0.05);
        text-align: center; margin-bottom: 25px;
    }
    </style>
    """, unsafe_allow_html=True)

st.markdown("<h1>🌿 Thai Carbon Daily Tracker</h1>", unsafe_allow_html=True)

# --- 2. เป้าหมายกิจกรรม ---
st.info("""
**🎯 เป้าหมายของกิจกรรมนี้:**
เพื่อให้คุณได้ตระหนักถึงผลกระทบจากการใช้ชีวิตประจำวัน และร่วมกันปรับลดการปล่อยก๊าซเรือนกระจก 
เพื่อสร้างความยั่งยืนและร่วมเป็นส่วนหนึ่งในการลดปัญหาภาวะโลกร้อน
""")

st.caption("🔍 **เกณฑ์การประเมิน:** อิงตามค่าเฉลี่ยประชากรไทยที่ปล่อยก๊าซประมาณ **10.4 kgCO2e ต่อวัน**")

# --- 3. ส่วนรับข้อมูล ---
col1, col2 = st.columns(2)
with col1:
    st.markdown("#### 🚗 การเดินทาง")
    transport = st.selectbox("พาหนะหลักวันนี้", 
        ["รถยนต์ส่วนตัว (น้ำมัน)", "รถยนต์ไฟฟ้า (EV)", "รถจักรยานยนต์", "รถไฟฟ้า (BTS/MRT)", "รถเมล์", "เดิน/ปั่นจักรยาน"])
    distance = st.number_input("ระยะทางรวม (กิโลเมตร)", min_value=0.0, step=1.0, value=10.0)
    
    st.markdown("#### 💡 พลังงาน")
    ac_hours = st.slider("เปิดแอร์วันนี้ (ชั่วโมง)", 0, 12, 0)

with col2:
    st.markdown("#### 🍽️ อาหาร 3 มื้อ")
    f1 = st.text_input("มื้อเช้า", placeholder="เช่น ข้าวเหนียวหมูปิ้ง")
    f2 = st.text_input("มื้อกลางวัน", placeholder="เช่น ข้าวกะเพราเนื้อ")
    f3 = st.text_input("มื้อเย็น", placeholder="เช่น สลัดผัก")

st.markdown("<br>", unsafe_allow_html=True)

# --- 4. ส่วนประมวลผล ---
if st.button("ประเมินผลลัพธ์และดูแนวทาง"):
    # คำนวณการเดินทาง
    ef_map = {
        "รถยนต์ส่วนตัว (น้ำมัน)": 0.218, "รถยนต์ไฟฟ้า (EV)": 0.05, 
        "รถจักรยานยนต์": 0.08, "รถไฟฟ้า (BTS/MRT)": 0.02, 
        "รถเมล์": 0.03, "เดิน/ปั่นจักรยาน": 0
    }
    c_transport = distance * ef_map[transport]
    
    # ตรวจสอบอาหาร
    all_food_text = (f1 + f2 + f3).lower()
    has_beef = any(x in all_food_text for x in ["เนื้อ", "beef"])
    
    def get_c(t):
        if not t: return 0.0
        text = t.lower()
        if any(x in text for x in ["เนื้อ", "beef"]): return 7.5
        if any(x in text for x in ["ผัก", "เจ", "สลัด"]): return 0.4
        return 1.2
    
    c_food = get_c(f1) + get_c(f2) + get_c(f3)
    c_ac = ac_hours * 0.51
    total = c_transport + c_food + c_ac

    # แสดงผล Card ผลลัพธ์
    st.markdown(f"""
        <div class="result-card">
            <h3 style="margin:0; color: #4b5563;">ปริมาณคาร์บอนรวมวันนี้</h3>
            <h1 style="margin:0; color: #16a34a; font-size: 50px;">{total:.2f} <span style="font-size: 20px; color: #9ca3af;">kgCO2e</span></h1>
            <p style="color: #6b7280;">ค่าเฉลี่ยคนไทยอยู่ที่ 10.4 kg ต่อวัน</p>
        </div>
    """, unsafe_allow_html=True)

    # --- 5. แนวทางลดก๊าซ (แยกสีชัดเจน) ---
    st.markdown("### 💡 แนวทางลดการปล่อยก๊าซ (Reduction)")
    
    # ด้านการเดินทาง
    st.markdown("**🚗 ด้านการเดินทาง**")
    if transport in ["รถยนต์ส่วนตัว (น้ำมัน)", "รถจักรยานยนต์"]:
        st.error(f"🚨 **ตรวจพบกิจกรรมคาร์บอนสูง:** การเดินทางด้วย {transport} ปล่อยก๊าซไอเสียมาก แนะนำให้ใช้รถสาธารณะหรือรวมกลุ่มเดินทาง (Carpool) ในวันที่ทำได้")
    elif transport in ["รถไฟฟ้า (BTS/MRT)", "รถเมล์", "รถยนต์ไฟฟ้า (EV)"]:
        st.info(f"ℹ️ **ทางเลือกที่ดี:** คุณเลือกใช้ {transport} ซึ่งช่วยลดการปล่อยก๊าซได้ดีกว่ารถน้ำมัน แนะนำให้ทำอย่างต่อเนื่องครับ")
    else:
        st.success("✅ **ยอดเยี่ยมมาก:** การเดินหรือปั่นจักรยานไม่มีการปล่อยก๊าซเรือนกระจกเลย ดีต่อทั้งโลกและสุขภาพของคุณ")

    # ด้านการบริโภคและแอร์
    c_food_col, c_ac_col = st.columns(2)
    with c_food_col:
        st.markdown("**🍽️ ด้านการบริโภค**")
        if has_beef:
            st.error("🚨 **ข้อแนะนำ:** ตรวจพบเมนูเนื้อวัว ซึ่งปล่อยก๊าซสูงกว่าเนื้อสัตว์อื่นถึง 6 เท่า หากเปลี่ยนเป็นเมนูไก่ ไข่ หรือโปรตีนจากพืช จะช่วยโลกได้มหาศาล")
        else:
            st.success("✅ **ข้อแนะนำ:** ไม่พบเมนูเนื้อวัวในวันนี้! แนะนำให้รักษามื้ออาหารที่ปล่อยคาร์บอนต่ำ และทานอาหารให้หมด (Zero Waste) เพื่อลดก๊าซมีเทนจากขยะ")

    with c_ac_col:
        st.markdown("**❄️ ด้านพลังงาน**")
        if ac_hours > 0:
            st.warning(f"⚠️ **ข้อแนะนำ:** มีการเปิดแอร์ {ac_hours} ชม. แนะนำให้ตั้งอุณหภูมิ 25-26 องศา พร้อมเปิดพัดลมช่วย จะช่วยประหยัดไฟและลดคาร์บอนได้มาก")
        else:
            st.success("✅ **ยอดเยี่ยม:** วันนี้คุณไม่เปิดแอร์เลย ช่วยลดภาระการผลิตไฟฟ้าจากโรงไฟฟ้าได้ดีเยี่ยม")

    # --- 6. แนวทางชดเชย ---
    st.markdown("---")
    st.markdown("### 🌿 แนวทางในการชดเชย (Offsetting)")
    st.write(f"ในกิจกรรมที่เลี่ยงไม่ได้ คุณสามารถเลือกวิธีชดเชยโดยการ **ปลูกต้นไม้** โดยไม้ยืนต้น 1 ต้น ดูดซับ CO2 ได้เฉลี่ย 9-15 กิโลกรัมต่อปี (วันนี้คุณปล่อยไป {total:.2f} kg ซึ่งเกือบเท่าความสามารถของต้นไม้ 1 ต้นในหนึ่งปีเต็ม) นอกจากนี้ควรสนับสนุนสินค้าที่มีตรา **คาร์บอนฟุตพริ้นท์** และร่วมประหยัดน้ำเพื่อลดพลังงานในระบบบำบัดเสีย")

st.markdown("---")
st.caption("อ้างอิงเกณฑ์ประเทศไทย 10.4 kgCO2e ต่อวัน | ข้อมูลสนับสนุนโดย อบก.")
