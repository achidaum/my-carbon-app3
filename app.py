import streamlit as st

# 1. ตั้งค่า Page และดีไซน์ CSS
st.set_page_config(page_title="Carbon Tracker Pro", layout="centered")

st.markdown("""
    <style>
    /* พื้นหลังไล่เฉดสี */
    .main {
        background: linear-gradient(135deg, #f0fdf4 0%, #dcfce7 100%);
    }
    
    /* ตกแต่งกล่อง Info ด้านบน */
    .stAlert {
        border-radius: 15px;
        border: none;
        background-color: rgba(255, 255, 255, 0.7);
        backdrop-filter: blur(10px);
        box-shadow: 0 4px 15px rgba(0,0,0,0.05);
    }

    /* ตกแต่งหัวข้อ */
    h1 {
        color: #065f46 !important;
        font-family: 'Kanit', sans-serif;
        text-shadow: 1px 1px 2px rgba(0,0,0,0.1);
    }

    /* ปรับแต่งปุ่ม */
    div.stButton > button:first-child {
        background: linear-gradient(90deg, #10b981 0%, #059669 100%);
        color: white;
        border-radius: 12px;
        border: none;
        padding: 0.6rem 2rem;
        font-weight: bold;
        width: 100%;
        transition: all 0.3s ease;
    }
    div.stButton > button:first-child:hover {
        transform: scale(1.02);
        box-shadow: 0 5px 15px rgba(16, 185, 129, 0.4);
    }
    </style>
    """, unsafe_allow_html=True)

# --- ส่วนหัวแอป ---
st.markdown("<h1 style='text-align: center;'>🌿 Carbon Daily Tracker</h1>", unsafe_allow_html=True)

st.info("""
**เป้าหมายของกิจกรรมนี้:** เพื่อให้คุณได้ **"ตระหนัก"** ถึงผลกระทบจากการใช้ชีวิตประจำวัน 
และเป็นจุดเริ่มต้นในการร่วมกัน **"ชดเชย"** หรือ **"ปรับลด"** การปล่อยก๊าซเรือนกระจกเพื่ออนาคตที่ยั่งยืน
""")

st.markdown("---")

# --- ส่วนกรอกข้อมูล (จัดเป็น Column เพื่อความสวยงาม) ---
col1, col2 = st.columns(2)

with col1:
    st.markdown("#### 🚗 การเดินทาง")
    transport = st.selectbox("พาหนะที่ใช้", 
        ["รถยนต์ส่วนตัว (น้ำมัน)", "รถยนต์ไฟฟ้า (EV)", "รถจักรยานยนต์", "รถไฟฟ้า (BTS/MRT)", "รถเมล์", "เดิน/ปั่นจักรยาน"])
    distance = st.number_input("ระยะทาง (กิโลเมตร)", min_value=0.0, step=1.0)

with col2:
    st.markdown("#### 🍔 อาหาร")
    food_input = st.text_input("เมนูวันนี้", placeholder="เช่น ข้าวกะเพราเนื้อวัว")
    st.markdown("#### 💡 พลังงาน")
    ac_hours = st.slider("เปิดแอร์ (ชม.)", 0, 12, 0)

st.markdown("<br>", unsafe_allow_html=True)

# --- ส่วนประมวลผล ---
if st.button("ประเมินผลลัพธ์และรับคำแนะนำ"):
    # (Logic การคำนวณคงเดิมเพื่อความแม่นยำ)
    ef_map = {"รถยนต์ส่วนตัว (น้ำมัน)": 0.218, "รถยนต์ไฟฟ้า (EV)": 0.05, "รถจักรยานยนต์": 0.08, "รถ
