import streamlit as st

# --- 1. ตั้งค่าหน้าตาแอป ---
st.set_page_config(page_title="Thai Carbon Daily Tracker", layout="centered")

# ดีไซน์สวยงาม (แก้จุดที่เครื่องหมายคำพูดหลุดเรียบร้อย)
st.markdown("""
    <style>
    .main { background-color: #f8fafc; }
    h1 { color: #15803d; text-align: center; font-family: 'Sarabun', sans-serif; }
    div.stButton > button:first-child {
        background: linear-gradient(90deg, #22c55e 0%, #16a34a 100%);
        color: white; border-radius: 12px; border: none; width: 100%; font-weight: bold; height: 3.5em;
    }
    .result-card {
        background: white; padding: 25px; border-radius: 20px; 
        border-top: 10px solid #22c55e; box-shadow: 0 10px 25px rgba(0,0,0,0.05);
        text-align: center;
    }
    .advice-card {
        background: #ffffff; padding: 15px; border-radius: 12px;
        border-left: 5px solid #10b981; margin-bottom: 10px;
        box-shadow: 0 2px 5px rgba(0,0,0,0.01);
    }
    </style>
    """, unsafe_allow_html=True)

st.markdown("<h1>🌿 Thai Carbon Daily Tracker</h1>", unsafe_allow_html=True)
st.info("เป้าหมาย: อิงเกณฑ์ค่าเฉลี่ยคนไทย 10.4 kgCO2e ต่อวัน เพื่อลดโลกร้อน")

# --- 2. ส่วนรับข้อมูล ---
col1, col2 = st.columns(2)
with col1:
    st.markdown("#### 🚗 การเดินทาง")
    transport = st.selectbox("พาหนะหลักวันนี้", 
        ["รถยนต์ส่วนตัว (น้ำมัน)", "รถยนต์ไฟฟ้า (EV)", "รถจักรยานยนต์", "รถไฟฟ้า (BTS/MRT)", "รถเมล์", "เดิน/ปั่นจักรยาน"])
    distance = st.number_input("ระยะทางรวม (กิโลเมตร)", min_value=0.0, step=1.0, value=10.0)
    
    st.markdown("#### 💡 พลังงาน")
    ac_hours = st.slider("เปิดแอร์วันนี้ (ชั่วโมง)", 0, 12, 0) # จำกัดแค่ 12 ชม.

with col2:
    st.markdown("#### 🍽️ อาหาร 3 มื้อ")
    f1 = st.text_input("มื้อเช้า", placeholder="เช่น ข้าวหมูแดง")
    f2 = st.text_input("มื้อกลางวัน", placeholder="เช่น กะเพราไก่")
    f3 = st.text_input("มื้อเย็น", placeholder="เช่น สลัดผัก")

st.markdown("<br>", unsafe_allow_html=True)

# --- 3. ส่วนประมวลผล ---
if st.button("ประเมินผลลัพธ์และดูแนวทาง"):
    # คำนวณการเดินทาง
    ef_map = {
        "รถยนต์ส่วนตัว (น้ำมัน)": 0.218, "รถยนต์ไฟฟ้า (EV)": 0.05, 
        "รถจักรยานยนต์": 0.08, "รถไฟฟ้า (BTS/MRT)": 0.02, 
        "รถเมล์": 0.03, "เดิน/ปั่นจักรยาน": 0
    }
    c_transport = distance * ef_map[transport]
    
    # คำนวณอาหาร
    def get_c(t):
        if not t: return 0.0
        t = t.lower()
        if any(x in t for x in ["เนื้อวัว", "beef"]): return 7.5
        if any(x in t for x in ["ผัก", "เจ", "สลัด"]): return 0.4
        return 1.2
    
    c_food = get_c(f1) + get_c(f2) + get_c(f3)
    c_ac = ac_hours * 0.51
    total = c_transport + c_food + c_ac

    # แสดงผล Card
    st.markdown(f"""
        <div class="result-card">
            <h3 style="margin:0; color: #4b5563;">ปริมาณคาร์บอนรวมวันนี้</h3>
            <h1 style="margin:0; color: #16a34a; font-size: 50px;">{total:.2f} <span style="font-size: 20px; color: #9ca3af;">kgCO2e</span></h1>
            <p style="color: #6b7280;">ค่าเฉลี่ยคนไทยอยู่ที่ 10.4 kg ต่อวัน</p>
        </div>
    """, unsafe_allow_html=True)

    if total <= 6.0:
        st.success("🟢 ระดับ: ดีเยี่ยม (Low) - คุณช่วยโลกได้มาก!")
    elif total <= 11.0:
        st.warning("🟡 ระดับ: ปานกลาง (Average) - อยู่ในเกณฑ์มาตรฐานคนไทย")
    else:
        st.error("🚨 ระดับ: ปล่อยก๊าซสูง (High) - วันนี้กิจกรรมของคุณส่งผลกระทบสูง")

    # --- 4. แนวทางลดและชดเชย ---
    st.markdown("---")
    st.markdown("### 💡 แนวทางลดการปล่อยก๊าซ (Reduction)")
    ca, cb = st.columns(2)
    with ca:
        st.markdown("<div class='advice-card'><b>🚗 การเดินทาง</b><br>• ใช้รถสาธารณะมากขึ้น<br>• ตรวจเช็คลมยางสม่ำเสมอ</div>", unsafe_allow_html=True)
    with cb:
        st.markdown("<div class='advice-card'><b>🍽️ การบริโภค</b><br>• ลดมื้อที่มีเนื้อวัวลง<br>• ทานอาหารให้หมดลดขยะอาหาร</div>", unsafe_allow_html=True)

    st.markdown("### 🌿 แนวทางชดเชย (Offsetting)")
    st.info(f"วันนี้คุณปล่อยไป {total:.2f} kg สามารถชดเชยได้ด้วยการปลูกต้นไม้ 1 ต้น (ดูดซับได้ ~10 kg/ปี) หรือเลือกใช้สินค้าที่มีฉลากลดโลกร้อน")

st.markdown("---")
st.caption("อ้างอิงเกณฑ์ประเทศไทย 10.4 kgCO2e ต่อวัน | ข้อมูลโดย อบก.")
