import streamlit as st
import pandas as pd

# 1. ตั้งค่า Page และดีไซน์ CSS
st.set_page_config(page_title="Carbon Tracker Pro", layout="centered")

st.markdown("""
    <style>
    .main {
        background: linear-gradient(135deg, #f0fdf4 0%, #dcfce7 100%);
    }
    .stAlert {
        border-radius: 15px;
        background-color: rgba(255, 255, 255, 0.7);
        backdrop-filter: blur(10px);
    }
    h1 {
        color: #065f46 !important;
        text-align: center;
    }
    div.stButton > button:first-child {
        background: linear-gradient(90deg, #10b981 0%, #059669 100%);
        color: white;
        border-radius: 12px;
        border: none;
        width: 100%;
        font-weight: bold;
        height: 3em;
    }
    </style>
    """, unsafe_allow_html=True)

# --- ส่วนหัวแอป ---
st.markdown("<h1>🌿 Carbon Daily Tracker</h1>", unsafe_allow_html=True)

st.info("""
**เป้าหมายของกิจกรรมนี้:** เราจัดทำขึ้นเพื่อให้คุณได้ **"ตระหนัก"** ถึงผลกระทบจากการใช้ชีวิตประจำวัน 
และเป็นจุดเริ่มต้นในการร่วมกัน **"ชดเชย"** หรือ **"ปรับลด"** การปล่อยก๊าซเรือนกระจกเพื่อสร้างอนาคตที่ยั่งยืนร่วมกัน
""")

st.caption("อ้างอิงเกณฑ์การคำนวณจาก องค์การบริหารจัดการก๊าซเรือนกระจก (อบก.)")

# --- ส่วนรับข้อมูล ---
col1, col2 = st.columns(2)

with col1:
    st.markdown("#### 🚗 การเดินทาง")
    transport = st.selectbox("เลือกประเภทพาหนะ", 
        ["รถยนต์ส่วนตัว (น้ำมัน)", "รถยนต์ไฟฟ้า (EV)", "รถจักรยานยนต์", "รถไฟฟ้า (BTS/MRT)", "รถเมล์", "เดิน/ปั่นจักรยาน"])
    distance = st.number_input("ระยะทาง (กิโลเมตร)", min_value=0.0, step=1.0)

with col2:
    st.markdown("#### 🍔 อาหาร")
    food_input = st.text_input("วันนี้ทานเมนูอะไร?", placeholder="เช่น ข้าวกะเพราเนื้อวัว")
    st.markdown("#### 💡 พลังงาน")
    ac_hours = st.slider("เปิดแอร์กี่ชั่วโมง?", 0, 12, 0)

st.markdown("<br>", unsafe_allow_html=True)

# --- ส่วนประมวลผล ---
if st.button("ประเมินผลลัพธ์"):
    # 1. คำนวณคาร์บอนการเดินทาง
    ef_map = {
        "รถยนต์ส่วนตัว (น้ำมัน)": 0.218, "รถยนต์ไฟฟ้า (EV)": 0.05, 
        "รถจักรยานยนต์": 0.08, "รถไฟฟ้า (BTS/MRT)": 0.02, 
        "รถเมล์": 0.03, "เดิน/ปั่นจักรยาน": 0
    }
    carbon_transport = distance * ef_map[transport]
    
    # 2. คำนวณคาร์บอนอาหาร
    food_carbon = 1.2
    food_category = "เนื้อสัตว์ทั่วไป (หมู/ไก่/ปลา/ไข่)"
    is_beef = False
    food_text = food_input.lower()
    
    if "เนื้อวัว" in food_text or ("เนื้อ" in food_text and not any(x in food_text for x in ["ไก่", "หมู", "ปลา", "เป็ด"])):
        food_carbon = 7.5
        food_category = "เนื้อวัว (ปศุสัตว์ขนาดใหญ่)"
        is_beef = True
    elif any(x in food_text for x in ["ผัก", "สลัด", "เจ", "มังสวิรัติ", "ผลไม้"]):
        food_carbon = 0.4
        food_category = "พืชผัก/มังสวิรัติ"
    
    # 3. คำนวณคาร์บอนไฟฟ้า
    carbon_ac = ac_hours * 0.51
    
    # 4. ยอดรวม
    total = carbon_transport + food_carbon + carbon_ac
    
    # --- แสดงผลลัพธ์แบบ Card ---
    st.markdown(f"""
        <div style="background: white; padding: 25px; border-radius: 20px; border-left: 10
